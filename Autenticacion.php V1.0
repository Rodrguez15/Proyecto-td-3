<?php
if (session_status() === PHP_SESSION_NONE) {
    session_start();
}

// Bloquear el acceso si no existe una sesión activa en el servidor
if (!isset($_SESSION['usuario_id'])) {
    header("Location: login.html");
    exit;
}
?>
